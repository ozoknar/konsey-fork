"""A2A client — peer registry; empty default → graceful inert.

Deliberately deferred, not a stray TODO — see council/a2a/__init__.py + KNOWN_ISSUES.md
for the evidence-based reasoning (A2A is the wrong layer for konsey's local-only
orchestration; ACP is the better fit but its Claude-Code-compatible bridge is alpha-stage)."""
from __future__ import annotations
